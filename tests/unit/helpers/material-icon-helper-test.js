import { materialIconHelper } from '../../../helpers/material-icon-helper';
import { module, test } from 'qunit';

module('Unit | Helper | material icon helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = materialIconHelper(42);
  assert.ok(result);
});
